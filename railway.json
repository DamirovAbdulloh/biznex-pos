{
  "$schema": "http://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": ". /app/.venv/bin/activate && python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn biznex.wsgi --log-file - --bind 0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
